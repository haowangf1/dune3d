Windows 构建（精简版）

一、推荐环境
- 使用 `MSYS2 UCRT64`
- 不要在 `PowerShell`、`cmd`、普通 `MSYS` 终端里执行构建命令

看到提示符里有 `UCRT64` 再继续，例如：
- `adminstrator@DESKTOP UCRT64 /d/workspace/dune3d`

二、更新 MSYS2
```bash
pacman -Syu
```
如果提示重开终端，就重开 `MSYS2 UCRT64` 后再执行一次：
```bash
pacman -Syu
```

三、安装依赖
```bash
pacman -S --needed \
  mingw-w64-ucrt-x86_64-gcc \
  mingw-w64-ucrt-x86_64-pkgconf \
  mingw-w64-ucrt-x86_64-gtkmm-4.0 \
  mingw-w64-ucrt-x86_64-glm \
  mingw-w64-ucrt-x86_64-eigen3 \
  mingw-w64-ucrt-x86_64-meson \
  mingw-w64-ucrt-x86_64-cmake \
  mingw-w64-ucrt-x86_64-python \
  mingw-w64-ucrt-x86_64-python-cairo \
  mingw-w64-ucrt-x86_64-python-gobject \
  mingw-w64-ucrt-x86_64-libepoxy \
  mingw-w64-ucrt-x86_64-harfbuzz \
  mingw-w64-ucrt-x86_64-freetype \
  mingw-w64-ucrt-x86_64-libpng \
  mingw-w64-ucrt-x86_64-opencascade \
  git zip unzip dos2unix wget
```

说明：
- `epoxy` 对应的包名是 `mingw-w64-ucrt-x86_64-libepoxy`
- `opencascade` 先直接安装仓库里的普通版本即可

四、进入项目目录
```bash
cd /d/workspace/dune3d
```

五、先清理错误环境变量
如果你的系统里设过 `CC`、`CXX`，Meson 可能会错误使用 Visual Studio 路径，导致配置失败。

先执行：
```bash
unset CC
unset CXX
unset CPP
unset LD
```

可检查当前编译器是否正确：
```bash
which gcc
which g++
echo $MSYSTEM
```
正常应类似：
- `/ucrt64/bin/gcc`
- `/ucrt64/bin/g++`
- `UCRT64`

六、配置并编译
```bash
rm -rf build
CC=gcc CXX=g++ meson setup build
meson compile -C build
```

七、运行
不要直接双击 exe，先在 `MSYS2 UCRT64` 终端里运行：
```bash
./build/dune3d.exe
```

八、这次实际遇到的问题
1. `meson: command not found`
- 原因：在 `PowerShell` 或错误终端里执行了命令
- 解决：切换到 `MSYS2 UCRT64`

2. Meson 误用 Visual Studio 编译器路径
- 现象：`Unknown compiler(s)`，并出现 VS 路径
- 原因：环境变量 `CC`、`CXX` 被错误设置成了一个目录
- 解决：
```bash
unset CC
unset CXX
unset CPP
unset LD
CC=gcc CXX=g++ meson setup build
```

3. `git` 找不到，导致 `make_version.py` 失败
- 现象：编译时生成 `version_gen.cpp` 失败
- 解决：安装并确认 `git` 可用
```bash
pacman -S --needed git
which git
git --version
```

4. `403` / `429` 镜像错误
- 一般是 MSYS2 镜像限流
- 只要 `pacman` 最终完成安装，通常可以继续
- 更新建议执行两次：
```bash
pacman -Syu
```

九、最短可用流程
```bash
pacman -Syu
pacman -S --needed \
  mingw-w64-ucrt-x86_64-gcc \
  mingw-w64-ucrt-x86_64-pkgconf \
  mingw-w64-ucrt-x86_64-gtkmm-4.0 \
  mingw-w64-ucrt-x86_64-glm \
  mingw-w64-ucrt-x86_64-eigen3 \
  mingw-w64-ucrt-x86_64-meson \
  mingw-w64-ucrt-x86_64-cmake \
  mingw-w64-ucrt-x86_64-python \
  mingw-w64-ucrt-x86_64-python-cairo \
  mingw-w64-ucrt-x86_64-python-gobject \
  mingw-w64-ucrt-x86_64-libepoxy \
  mingw-w64-ucrt-x86_64-harfbuzz \
  mingw-w64-ucrt-x86_64-freetype \
  mingw-w64-ucrt-x86_64-libpng \
  mingw-w64-ucrt-x86_64-opencascade \
  git zip unzip dos2unix wget
cd /d/workspace/dune3d
unset CC
unset CXX
unset CPP
unset LD
rm -rf build
CC=gcc CXX=g++ meson setup build
meson compile -C build
```
